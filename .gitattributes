# Keep repository text stable across Windows, macOS, and Linux contributors.
* text=auto eol=lf

# Windows helper scripts should retain native line endings when added later.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
